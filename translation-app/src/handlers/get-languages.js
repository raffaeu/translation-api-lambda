/**
 * A simple example includes a HTTP post method to add one item to a DynamoDB table.
 */
exports.getLanguagesHandler = async (event) => {
    const response = {
        statusCode: 200,
        body: JSON.stringify('body')
    };

    return response;
};
